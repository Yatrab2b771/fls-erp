# Unset locally — Vite's dev server proxies /api to localhost:4000 (see
# vite.config.ts). Set this only for the production build (e.g. as a
# Vercel project env var) to the deployed API's origin, no trailing slash.
VITE_API_URL=
